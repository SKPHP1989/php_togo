// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("togo", "for togo support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("togo", "enable togo support", "no");

if (PHP_TOGO != "no") {
	EXTENSION("togo", "togo.c");
}

